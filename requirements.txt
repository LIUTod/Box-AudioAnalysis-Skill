# Generic audio analysis - Python dependencies

# 核心依赖
funasr>=1.3.0
torch>=2.0.0
torchaudio>=2.0.0
requests>=2.28.0
huggingface-hub>=0.23.0

# RAG：本地 Chroma + BGE 中文向量（设备自适应 CUDA > MPS > CPU，见 knowledge_rag.py）
chromadb>=0.4.24
sentence-transformers>=2.2.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0

# 流式分析：在 config.json 中配置 llm.provider=openai 时使用（可选）
openai>=1.0.0

# 可选：用于更快推理（暂不需要）
# funasr-onnx>=0.4.0
# onnxruntime>=1.7.0